{%- comment -%}
  Search Hint State Component
  
  Displays a hint message when only 1 character is typed
  - Accessible with ARIA labels
  - Informative message for users
  - Consistent styling with other search states
{%- endcomment -%}

<div class="search-hint-content" role="status" aria-label="{{ 'drawer-group.multi.search.hint' | t | default: 'Search hint' }}">
  <div class="search-hint-icon">
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="icon-search-hint" aria-hidden="true">
      <path stroke-linecap="round" stroke-linejoin="round" d="M11.25 11.25l.041-.02a.75.75 0 011.063.852l-.708 2.836a.75.75 0 001.063.853l.041-.021M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-9-3.75h.008v.008H12V8.25z" />
    </svg>
  </div>
  
  <div class="search-hint-text">
    <p class="search-hint-message">{{ 'drawer-group.multi.search.type_more_characters' | t | default: 'Please type at least 2 characters' }}</p>
    <p class="search-hint-description">{{ 'drawer-group.multi.search.hint_description' | t | default: 'Continue typing to search for products' }}</p>
  </div>
</div>

<style>
  .search-hint-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    text-align: center;
    min-height: 200px;
  }

  .search-hint-icon {
    margin-bottom: 1rem;
    color: var(--color-text-light, #666);
  }

  .icon-search-hint {
    width: 48px;
    height: 48px;
  }

  .search-hint-text {
    color: var(--color-text, #333);
  }

  .search-hint-message {
    font-size: 1rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
    color: var(--color-primary, #000);
  }

  .search-hint-description {
    font-size: 0.875rem;
    color: var(--color-text-light, #666);
    margin: 0;
  }

  /* Smooth transition animations */
  .search-hint-state {
    transition: opacity 0.3s ease, transform 0.3s ease;
  }

  .search-hint-state[style*="display: none"] {
    opacity: 0;
    transform: translateY(-10px);
  }

  .search-hint-state:not([style*="display: none"]) {
    opacity: 1;
    transform: translateY(0);
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    .search-hint-state {
      transition: opacity 0.15s ease;
    }
  }
</style></content>
</invoke>